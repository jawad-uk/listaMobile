// Generated by CoffeeScript 1.6.3
var NewTodoViewView, _ref,
  __hasProp = {}.hasOwnProperty,
  __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor(); child.__super__ = parent.prototype; return child; };

module.exports = NewTodoViewView = (function(_super) {
  __extends(NewTodoViewView, _super);

  function NewTodoViewView() {
    _ref = NewTodoViewView.__super__.constructor.apply(this, arguments);
    return _ref;
  }

  NewTodoViewView.prototype.className = 'new_todo_view';

  NewTodoViewView.prototype.template = require('./templates/new_todo');

  NewTodoViewView.prototype.id = 'new-todo-view';

  NewTodoViewView.prototype.events = {
    'keypress #new-todo': 'createOnEnter',
    'click #new-todo-submit': 'createOnClick',
    'focusin #new-todo': 'showButton',
    'focusout #new-todo': 'hideButton'
  };

  NewTodoViewView.prototype.newAttributes = function() {
    var attributes;
    attributes = {
      order: application.todos.nextOrder()
    };
    if (this.$('#new-todo').val()) {
      attributes.content = this.$('#new-todo').val();
    }
    return attributes;
  };

  NewTodoViewView.prototype.createOnEnter = function(event) {
    if (event.keyCode !== 13) {
      return;
    }
    application.todos.create(this.newAttributes());
    return this.$('#new-todo').val('');
  };

  NewTodoViewView.prototype.createOnClick = function(event) {
    console.log('registered click on new todo button');
    application.todos.create(this.newAttributes());
    return $('#new-todo').val('');
  };

  NewTodoViewView.prototype.showButton = function() {
    var button;
    button = this.$('#new-todo-submit');
    button.addClass('display');
    return $('#new-todo').focus();
  };

  NewTodoViewView.prototype.hideButton = function() {
    var button;
    button = $('#new-todo-submit');
    return button.removeClass('display');
  };

  return NewTodoViewView;

})(Backbone.View);
